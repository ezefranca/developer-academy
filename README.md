# Developer Academy Pin Generator

Open `index.html` in a browser (or serve the folder).

Recommended: `python3 -m http.server` in this directory to avoid some browser security restrictions.

Outputs:
- Download share card PNG
- Download pin-only PNG with transparent background

Assets live in `/assets`:
- `faceid.png`
- `hat.svg`
- `grid.svg`
